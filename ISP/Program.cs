using System;

namespace ISP
{
    /*
     *  A client should not depend on an Interface it doesn't use!
     *  Break the application apart to lots of small interfaces!
     *  Library example, DVD doesn't have pages, audio books doesn't either.
     * 
     * 
     * 
     */

    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
